const errorConstants = {
    tokenExpired: "Token has expired",
    invalidToken: "Invalid token",
};

const successConstants = {};

export const passwordResetConstants = {
    error: errorConstants,
    success: successConstants,
};
